# 第一阶段数据构造指南：统一母样本池（FULL / E+）

## 1. 结论：方案是否合理

整体方案合理，且执行顺序正确：先从三个数据集中构造“问题—答案—完整上下文—关键证据单元—推理链”的统一母样本池，验证完整上下文确实可回答，再进入证据删除阶段。第一阶段不生成拒答或澄清文本，也不构造 `MISSING-1`。

不过，实际实现时需要做以下修正和补强：

1. **`context_full` 必须保留完整候选上下文，而不只是支撑文档。** HotpotQA 的 8 个干扰段落、MuSiQue 的非支撑段落都应保留，并用 `is_supporting_doc` 标记；否则无法在后续研究干扰鲁棒性，也无法检测答案在干扰段落中重复泄漏。
2. **“完整上下文含答案字符串”不等于“证据足够”。** 必须分别记录答案可定位、支撑链完整、规则验证通过、模型回答正确四类状态。
3. **不要把模型回答错误直接等同于母样本无效。** 建议形成两个池：
   - `parent_valid`：数据结构和证据经规则/人工确认有效；
   - `parent_model_pass`：在 `parent_valid` 中，指定基线模型对完整证据回答正确的子集。后续构造缺失证据训练对时使用后者，但保留前者以免引入不透明的模型能力偏差。
4. **HotpotQA 的 supporting facts 没有直接给出 hop 顺序。** 对 bridge 样本可根据桥接实体、标题及句间引用恢复顺序；无法可靠恢复时应标记 `hop: null` 和 `hop_order_status: unresolved`，不能凭数组顺序赋值。
5. **NQ 的答案句只能标记为候选证据。** 使用 `support_source: answer_span_sentence`，并设置 `support_necessity: unverified`；不能在第一阶段宣称它是唯一必要证据。
6. **过滤必须可审计。** 不要只输出过滤后的文件；每条被过滤样本都应记录机器可读的 `reason_code`，统计可从日志重算。

## 2. 第一阶段边界

### 本阶段做什么

- 解析 HotpotQA distractor、MuSiQue-Ans、原始 Natural Questions；
- 首轮每个数据集最多抽取 1,000 条候选样本；
- 转换为统一 schema；
- 执行结构校验、答案定位、泄漏检测和规则过滤；
- 输出完整母样本、过滤后母样本、拒绝记录和统计报告；
- 生成每个数据集 50 条的人工抽检表；
- 在指定基线模型可用时，评测 `Acc(q, E+)` 并生成模型通过子集。

### 本阶段不做什么

- 不删除 supporting facts；
- 不构造 `E-`；
- 不生成“拒答 + 澄清”文本；
- 不混入 MuSiQue-Full 自带的不可回答样本；
- 不训练或微调模型；
- 不处理 HotpotQA comparison、MuSiQue 3/4 跳、NQ 表格/列表/yes-no 等扩展类型。

## 3. 开工前需要提供的内容

### 3.1 必须提供：本地原始数据文件

请将数据放入 `F:\zyz\shujv_preview\raw`，推荐结构如下：

```text
F:\zyz\shujv_preview\
├─ raw\
│  ├─ hotpotqa\
│  │  ├─ hotpot_train_v1.1.json
│  │  └─ hotpot_dev_distractor_v1.json
│  ├─ musique\
│  │  ├─ musique_ans_v1.0_train.jsonl
│  │  └─ musique_ans_v1.0_dev.jsonl
│  └─ natural_questions\
│     ├─ train\
│     │  └─ *.jsonl.gz
│     └─ dev\
│        └─ *.jsonl.gz
├─ config\
└─ output\
```

文件名允许不同，但必须明确每个文件对应的数据集、版本和 split。压缩文件可保留原样，无须人工解压。

三个数据集的最低输入要求：

| 数据集 | 必须是哪个版本 | 必须包含的关键字段 |
|---|---|---|
| HotpotQA | distractor train/dev | `_id`, `question`, `answer`, `type`, `context`, `supporting_facts` |
| MuSiQue | MuSiQue-Ans（answerable） | `id`, `question`, `answer`, `paragraphs`, `question_decomposition` 或等价分解字段 |
| Natural Questions | 原始 NQ，而非 NQ-Open | `example_id`, `question`, `document_tokens`, `long_answer_candidates`, `annotations`（含 long/short answer 与 yes/no） |

如果当前 `shujv_preview` 中只有截断预览或二次加工文件，也可以先提供；但应同时说明预览生成方式及是否保留上述字段。缺失关键字段时，只能做解析原型，不能形成可验收母样本。

### 3.2 必须确认：数据使用范围

请提供或确认以下五项配置；若不指定，将使用括号中的默认值：

1. 每个数据集候选抽取量（默认：各 1,000 条，先 train 后 dev）；
2. 随机种子（默认：`20260821`）；
3. 答案最大长度（默认：空白分词后不超过 10 tokens，且不超过 80 字符）；
4. 输出语言（默认：字段名和报告为中文，原始问题、答案、证据文本不翻译）；
5. 是否允许脚本安装或使用现有 Python 文本处理依赖（默认：优先使用现有依赖，不修改全局环境）。

### 3.3 完成 `Acc(q, E+)` 还需要：基线模型配置

纯解析、规则过滤和人工抽检表生成不需要模型即可开始。但要完成“完整证据输入模型后的准确率”验收，需要以下三种方式中的一种：

- 用户提供可调用的模型 API/本地推理端点及模型名；或
- 用户指定当前已有的本地模型与启动方式；或
- 用户同意先只生成标准化评测输入，由用户在外部运行后回填预测结果。

还需确认评测协议：是否允许模型使用外部知识。默认设置为**封闭上下文问答**，提示模型只能根据给定上下文作答；解码使用确定性参数（如 temperature 0）。

密钥不要写入数据文件、配置文件或聊天正文。应通过环境变量或本机安全凭据注入。

### 3.4 建议提供但不是开工阻塞项

- 期望使用的答案归一化规则或现有评测脚本；
- 数据集下载来源、版本号、校验值或许可证记录；
- 计划用于最终实验的基线模型名称；
- 允许保留的元数据字段和磁盘预算；
- 是否需要对抽检表加入人工标注者、日期、备注等项目管理字段。

## 4. 统一母样本 schema（建议版）

统一 JSONL 每行一条，建议使用以下结构：

```json
{
  "schema_version": "parent_pool.v1",
  "id": "hotpotqa_xxx",
  "source": "hotpotqa",
  "source_id": "原始样本ID",
  "question": "原始问题",
  "answers": ["标准答案", "可接受别名"],
  "context_full": [
    {
      "doc_id": "稳定文档ID或标题",
      "title": "页面标题",
      "text": "段落拼接文本",
      "sentences": ["句子1", "句子2"],
      "is_supporting_doc": true,
      "source_position": 0
    }
  ],
  "support_units": [
    {
      "unit_id": "doc_1_sent_2",
      "doc_id": "doc_1",
      "sentence_id": 2,
      "text": "关键证据文本",
      "hop": 1,
      "support_source": "gold_annotation",
      "support_necessity": "unverified"
    }
  ],
  "decomposition": [],
  "num_hops": 2,
  "reasoning_type": "bridge",
  "validation": {
    "schema_valid": true,
    "answer_found_in_context": true,
    "answer_found_in_support": true,
    "support_chain_complete": true,
    "hop_order_status": "resolved",
    "model_checked": false,
    "model_answer_correct": null,
    "human_checked": false
  },
  "metadata": {
    "original_split": "train",
    "answer_type": "entity",
    "has_question_answer_leakage": false,
    "has_distractor_answer_leakage": false,
    "raw_locator": "相对路径或分片编号",
    "raw_index": 0
  }
}
```

### Schema 约束

- `context_full` 按原始顺序保留所有候选段落，不能只保留支撑段落；
- `answers` 至少一个非空字符串；别名只能来自数据集标注或明确规则，不能由模型随意生成；
- HotpotQA/MuSiQue 使用 `gold_annotation`；NQ 候选答案句使用 `answer_span_sentence`；
- 无法可靠确定 hop 时使用 `null`，同时记录未解析状态；
- 保留 `source_id + raw_locator + raw_index`，确保能追溯到原始样本；
- 所有派生文本必须保留原始字符，不在母样本内做翻译或改写；
- JSONL 使用 UTF-8 编码，每行可独立解析。

## 5. 三个数据集的具体处理规则

### 5.1 HotpotQA（第一优先级）

首版仅保留：

- distractor 配置；
- `type == bridge`；
- 非 yes/no；
- 两个不同的支撑文档；
- 2–4 条有效 supporting sentences；
- 简短实体型答案。

处理步骤：

1. 原样保留 10 个候选段落及句子边界；
2. 用 `(title, sentence_id)` 精确解析 supporting facts；
3. 验证标题存在、句子下标有效、支撑句文本非空；
4. 检查标准答案是否出现在完整上下文、支撑句、非支撑句、标题和问题中；
5. 根据桥接实体或标题引用尝试恢复 hop 顺序；
6. 无法确定顺序则保留样本但标记为未解析，过滤阶段再决定是否进入首版池。

特别注意：答案出现在 supporting sentence 是强信号，但第一跳通常只提供桥接实体，不能要求每条支撑句都包含最终答案。

### 5.2 MuSiQue-Ans（第二优先级）

首版仅保留：

- answerable 样本；
- 2-hop；
- 有完整 question decomposition；
- 每个分解步骤能映射到有效支撑段落；
- 简短答案。

处理步骤：

1. 保留全部 paragraphs，并根据标注设置 `is_supporting_doc`；
2. 解析分解问题、分步答案和依赖关系；
3. 将支撑段落与 hop 对齐；
4. 检查最终答案在完整上下文中的可定位性；
5. 检查中间答案或桥接实体在推理链中的连通性；
6. 对字段版本差异建立显式适配器，禁止静默丢弃 decomposition。

MuSiQue 的证据标注可能在段落级而非句子级。首版应忠实保留原标注粒度，不要用未经验证的句子切分冒充 gold supporting sentence。

### 5.3 Natural Questions（第三优先级）

首版仅保留：

- long answer 存在；
- short answer 存在且只有一个 span；
- `yes_no_answer == NONE`；
- long answer candidate 为普通 `<P>` 段落；
- short answer 完整位于 long answer 内；
- 长答案长度在配置上限内；
- 简短、非高歧义的答案。

处理步骤：

1. 根据 token offsets 恢复 long-answer 段落和 short-answer 文本；
2. 保留 token/字符 offset 映射，避免清理 HTML 后位置不可追溯；
3. 将包含 short-answer span 的句子标为候选支撑单元；
4. 使用 `support_source: answer_span_sentence`；
5. 使用 `support_necessity: unverified`；
6. 过滤表格、列表、多个 short spans、yes/no 及解析不稳定样本。

NQ 句子切分后必须验证 short-answer 的字符区间仍落在该句内。若 HTML 清理导致 offset 无法稳定映射，应拒绝该样本并记录原因。

## 6. 过滤与拒绝原因编码

建议至少实现以下可重算的 `reason_code`：

| 原因码 | 含义 |
|---|---|
| `EMPTY_QUESTION` | 问题为空 |
| `MISSING_ANSWER` | 无明确答案 |
| `ANSWER_TOO_LONG` | 答案超过首版限制 |
| `ANSWER_IN_QUESTION` | 归一化答案直接出现在问题中 |
| `ANSWER_NOT_IN_FULL_CONTEXT` | 完整上下文找不到答案或认可别名 |
| `EMPTY_SUPPORT` | 支撑证据为空 |
| `BROKEN_SUPPORT_POINTER` | 文档或句子指针无效 |
| `INCOMPLETE_CHAIN` | 多跳链不完整 |
| `UNRESOLVED_HOP_ORDER` | 首版要求下无法恢复 hop 顺序 |
| `DISTRACTOR_ANSWER_LEAKAGE` | 答案在非支撑证据中重复出现 |
| `DUPLICATE_SUPPORT_DISTRACTOR` | 支撑内容与干扰内容完全重复 |
| `UNSUPPORTED_FORMAT` | 表格、列表、图片或特殊 HTML |
| `AMBIGUOUS_QUESTION` | 问题明显歧义（通常需人工确认） |
| `OFFSET_MAPPING_FAILED` | NQ token/字符位置映射失败 |
| `PARSE_ERROR` | 原始记录无法解析 |

同一条样本可以记录多个原因，但需指定一个 `primary_reason` 便于统计。

## 7. 完整证据验证流程

验证分四层进行，不应混成一个布尔值：

1. **结构验证**：schema、指针、offset、字段类型均合法；
2. **可定位验证**：答案在完整上下文中可定位，且记录位于支撑证据还是干扰证据；
3. **证据链验证**：多跳依赖连通、支撑单元可恢复；NQ 只验证候选答案句，不声称必要性；
4. **可回答性验证**：基线模型在封闭上下文条件下正确作答，外加人工抽检。

答案评分建议同时报告：

- Exact Match（归一化后完全一致）；
- Token F1；
- Alias Match；
- 人工裁决率（自动评分无法处理时）。

第一版建议验收门槛：

- 100% 输出记录通过 JSON Schema；
- 100% 支撑指针/offset 可解析；
- 100% 保留样本的答案可在完整上下文中定位；
- HotpotQA/MuSiQue 首版保留样本的推理链可恢复；
- 每个数据集随机人工抽检 50 条；其中“完整上下文可可靠回答”和“证据标注合理”建议均不低于 90%；
- `Acc(q, E+)` 按数据集分别报告，不只报告混合平均值；进入后续成对构造的单条样本必须通过指定基线模型的完整证据回答测试或人工裁决。

阈值是首版工程门槛，可在看到真实数据质量后调整，但调整必须写入版本化配置和报告。

## 8. 第一阶段应交付的文件

建议输出到 `F:\zyz\shujv_preview\output\parent_pool_v1`：

```text
parent_pool_v1\
├─ data\
│  ├─ hotpotqa_parent.jsonl
│  ├─ musique_parent.jsonl
│  ├─ nq_parent.jsonl
│  ├─ hotpotqa_parent_filtered.jsonl
│  ├─ musique_parent_filtered.jsonl
│  ├─ nq_parent_filtered.jsonl
│  ├─ hotpotqa_parent_model_pass.jsonl
│  ├─ musique_parent_model_pass.jsonl
│  └─ nq_parent_model_pass.jsonl
├─ rejected\
│  ├─ hotpotqa_rejected.jsonl
│  ├─ musique_rejected.jsonl
│  └─ nq_rejected.jsonl
├─ review\
│  ├─ hotpotqa_manual_review.csv
│  ├─ musique_manual_review.csv
│  └─ nq_manual_review.csv
├─ reports\
│  ├─ dataset_statistics.json
│  ├─ validation_report.md
│  └─ model_eplus_metrics.json
├─ config\
│  └─ build_config.yaml
├─ schema\
│  └─ parent_pool.schema.json
└─ manifests\
   ├─ input_manifest.json
   └─ output_checksums.sha256
```

如果暂未提供基线模型，三个 `*_model_pass.jsonl` 和 `model_eplus_metrics.json` 暂不生成，其余工作不受影响。

## 9. 人工抽检表需要回答的问题

每个数据集固定随机种子抽取 50 条，至少包含以下字段：

- `full_context_answerable`: 完整上下文能否可靠回答；
- `answer_correct`: 标准答案是否正确；
- `support_relevant`: 支撑单元是否真正支撑答案；
- `chain_complete`: 多跳链是否完整；
- `equivalent_evidence_exists`: 是否有其他等价证据；
- `deletion_likely_unanswerable`: 删除该单元是否可能使问题不可回答；
- `ambiguity`: 问题是否歧义；
- `reviewer_note`: 说明与裁决依据。

其中最后两项关于删除的判断只用于为第二阶段做准备，不在本阶段实际删除证据。

## 10. 可复现性和数据安全要求

- 原始数据目录只读，所有派生数据写入独立输出目录；
- 记录输入文件大小、修改时间和 SHA-256；
- 固定随机种子并保存实际抽中样本 ID；
- 保存代码版本、配置版本和 schema 版本；
- 写出临时文件后采用原子替换，避免中途中断留下伪完整输出；
- 不把 API 密钥、访问令牌写进 JSONL、报告或日志；
- 每次构建生成唯一 run ID，报告中记录开始时间、结束时间和异常计数。

## 11. 收到数据后的实际执行顺序

1. 只读盘点文件、版本、split、数量、体积和字段；
2. 生成 `input_manifest.json` 与字段兼容性报告；
3. 每个数据集抽取少量记录做解析冒烟测试；
4. 实现统一 schema 与三套适配器；
5. 运行每个数据集最多 1,000 条的母样本构造；
6. 执行规则过滤并输出逐条拒绝原因；
7. 生成统计报告和 3×50 人工抽检表；
8. 用户完成或确认人工抽检；
9. 有模型配置时运行 `Acc(q, E+)`；
10. 冻结 `parent_pool_v1`，再讨论第二阶段的可控删除策略。

## 12. 当前状态与最小开工清单

截至本指南生成时，`F:\zyz` 目录为空，未发现 `F:\zyz\shujv_preview`，当前工作区也未发现该目录。因此现在还不能检查真实字段或开始解析。

要开始第一阶段，用户最少需要：

1. 将三个数据集文件放到 `F:\zyz\shujv_preview\raw`，或告知它们的实际本地绝对路径；
2. 确认文件分别属于 HotpotQA distractor、MuSiQue-Ans、原始 Natural Questions；
3. 确认是否采用默认配置：每个数据集 1,000 条、随机种子 `20260821`；
4. 若本轮就要完成 `Acc(q, E+)`，再提供基线模型的本地调用方式或安全的 API 配置方式。

前三项齐备后即可开始解析、统一格式、过滤、统计和生成抽检表；基线模型不是开始构造母样本的前置条件，但它是完成完整证据准确率验收的前置条件。
